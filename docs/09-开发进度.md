# TokenHub 开发进度

本文是需求落地的进度账本。每完成一个里程碑必须记录：范围、证据、端到端验证结果、PR。验证失败不得标记完成，必须先修复。

关联文档：`08-TokenHub-P0开发拆分与验收顺序.md`。优先级：`M0 -> M1 -> M2 -> M3`，然后 `M4/M5/M6` 可并行，最后 `M7`。

## 文档与分支基线

产品、架构、数据模型和验收标准以 **`release/v0.1.0`** 上的 `docs/00`–`08` 为准。已将 `main` 上的 M0 以及 M1/M2 功能分支合入该基线；后续里程碑的 PR 一律对准 `release/v0.1.0`，不再以 `main` 为默认合入目标。

核对结果（2026-08-29）：合入时 `00`–`08` 与 `origin/release/v0.1.0` 内容哈希一致。M3 按 `08` 第 3 节要求，把新增账务表前缀、兑换码和管理结算接口同步写进了 `05`/`06`/`08`。

## 总览

| 里程碑 | 状态 | 验证 | PR |
| --- | --- | --- | --- |
| M0 基础工程与安全底座 | 验证通过，CI 全绿，待仓库权限合并 | 2026-08-29 本地 e2e + GitHub CI | https://github.com/tokpath/tokstudio/pull/1 |
| M1 身份、渠道与品牌 | 验证通过，待合并 | 2026-08-29 本地 e2e 通过 | https://github.com/tokpath/tokstudio/pull/2 |
| M2 Provider、模型与 Bifrost 文本网关 | 验证通过，已合入 release 基线 | 2026-08-29 本地 e2e 通过 | https://github.com/tokpath/tokstudio/pull/3 |
| M3 钱包、预授权与 usage 结算 | 验证通过，待合并到 release/v0.1.0 | 2026-08-29 本地 go test + e2e_m0/m1/m2/m3 | https://github.com/tokpath/tokstudio/pull/4 |
| M4 Seedance 视频与媒体任务 | 验证通过，待合并到 release/v0.1.0 | 2026-08-29 本地 go test + e2e_m0/m1/m2/m3/m4 | https://github.com/tokpath/tokstudio/pull/5 |
| M5 套餐、订阅与支付 | 验证通过，CI 全绿，待合并到 release/v0.1.0 | 2026-08-29 本地 e2e + GitHub CI `5febdf7`/`b4ef09e` | https://github.com/tokpath/tokstudio/pull/6 |
| M6 分销、佣金与渠道运营 | 验证通过，CI 全绿，待合并到 release/v0.1.0 | 2026-08-29 本地 e2e + GitHub CI `74216a6` | https://github.com/tokpath/tokstudio/pull/7 |
| M7 运营、运维与上线加固 | 验证通过，待合并到 release/v0.1.0 | 2026-08-29 本地 go test + e2e_m0–m7 | https://github.com/tokpath/tokstudio/pull/8 |

状态含义：`未开始` / `进行中` / `验证失败` / `验证通过，待合并` / `已完成并合并`。

## M0 基础工程与安全底座

文档验收已用本地进程 + CI 核对。GitHub Actions `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿。Agent 的 GitHub token 对 `PUT /pulls/1/merge` 返回 403，无法代为合并；PR 已从草稿改为 ready。

## M1 身份、渠道与品牌

文档验收：

- 用户注册后渠道归属不可自行切换；
- 不同渠道数据隔离；
- 管理员权限和审计生效；
- 四类入口可在同一前端代码库中按域名/角色正确渲染；
- OEM 站点可按品牌配置渲染；
- 直接调用未授权 API 返回 403。

实现清单：

- [x] 邮箱密码注册/登录 + HttpOnly Cookie
- [x] 邮箱验证码（开发环境可回显 `dev_code`，生产不回显）
- [x] Google OAuth：暂存推广码；无 Client ID 时走 mock（`mock:email`）
- [x] A/B/C 渠道、官方/OEM 品牌、推广码、唯一归因
- [x] 用户自助改渠道固定 403；平台管理员改归因写审计
- [x] 渠道管理员只能列出本渠道用户
- [x] 公共站 / 用户控制台 / 渠道控制台 / 平台管理 + 品牌化文档
- [x] `scripts/e2e_m1.sh`

### 验证记录（2026-08-29）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `go test ./...` | 通过 | `TestM1IdentityIsolation`：A/B 不同渠道、自助切换 403、渠道列表不泄漏、OEM 品牌 |
| `web` Vitest + `next build` | 通过 | 门户路径映射与 OEM theme token |
| `scripts/e2e_m0.sh` | 通过 | M1 代码未破坏 M0 健康/RBAC/审计/Outbox |
| `scripts/e2e_m1.sh` | 通过 | 注册归因、403 切换、渠道隔离、OTP、Google mock 走 OEM、六个页面渲染 |

## M2 Provider、模型与文本网关

文档验收：同一公开模型多 Provider、429/5xx fallback、流式开始后不切换、attempt 可追踪、不支持参数 4xx、测试 Key 可跑通示例、OEM 文档展示品牌 Base URL 和模型白名单。

实现清单：

- [x] catalog 模块：Provider/模型/映射/价格版本/路由组/渠道白名单
- [x] 测试 Adapter（Bifrost 接口已预留，无 URL 时不作为候选）
- [x] OpenAI Chat / Responses / Anthropic Messages
- [x] SSE 流式；429/5xx fallback；logit_bias 返回 400
- [x] tool calls / JSON schema / vision / reasoning：能透传的回结构化结果，不能的 4xx；reasoning_tokens 计入账单
- [x] request/attempt 落库
- [x] 用户 API Key（密文+hash）
- [x] `scripts/e2e_m2.sh`

### 验证记录（2026-08-29）

`go test ./...`、`scripts/e2e_m2.sh` 通过：echo-primary 成功、强制 429 后落到 echo-backup、两次 attempt、logit_bias=400、SSE 含 data:、OEM docs 含 `tokenhub/oem-demo`。

M3 已把预授权接到网关：调用 Adapter 前 `Reserve`，余额不足返回 402。M2 e2e 会先兑换 `THE2E`。

## M3 钱包、预授权与 usage 结算

文档验收：并发请求不双扣；余额不足不调用上游；重复 usage/webhook 幂等；旧价格账单不变；退款和佣金冲正可重算。

实现清单：

- [x] `billing` 模块独立 migration：钱包、流水、额度、充值、兑换码、预授权、usage、客户账单、成本、佣金
- [x] 预授权行锁；不足则 402 且不调上游
- [x] usage 按价格快照结算；缺 usage 进入 `pending_reconciliation`
- [x] 人工充值 + 兑换码 `THE2E` / `THCREDIT10`
- [x] 退款反向流水；佣金可冲正重算
- [x] 客户账单与成本报表
- [x] `scripts/e2e_m3.sh`、`TestM3BillingInvariants`

### 验证记录（2026-08-29）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `go test ./...` | 通过 | `TestM3BillingInvariants`：402 不打上游、并发预授权 1 成 1 败且总额守恒、重复 usage 同一 charge、改价后旧账单不变、退款恢复余额并冲正佣金 |
| `web` Vitest | 通过 | 钱包 micro-USD 展示 |
| `scripts/e2e_m0.sh` `e2e_m1.sh` `e2e_m2.sh` | 通过 | M3 未破坏既有切片；M2 先兑换 `THE2E` |
| `scripts/e2e_m3.sh` | 通过 | 空钱包 402、兑换入账、快照结算、usage 回放幂等、改价不改旧账、缺 usage 待对账、退款+佣金重算、人工确认充值、报表 |

`/healthz` 版本 `0.1.0-m3`；`/readyz` 含 `billing` migration。Agent 无合并权限，需人工把 **#4** 合进 `release/v0.1.0`。

## M4 Seedance 视频与媒体任务

文档验收：任务拿到上游 ID 后不重复提交；回调可重试且不重复结算；失败/取消释放正确预授权；结果只能通过签名 URL 获取。

实现清单：

- [x] 独立 `media` 模块与 migration：`media_jobs` / `media_assets` / `media_callback_events`
- [x] 目录种子：`bytedance/seedance-1.0`、`tokenhub/image-demo`；Provider `ark-seedance` / `openrouter-seedance`
- [x] 无 Ark/OpenRouter Base URL 时走沙箱 TestAdapter
- [x] `POST/GET /v1/videos`、`/content`、`/cancel`；`POST /v1/images/generations|edits`
- [x] `POST /v1/media/callbacks`：HMAC 验签、按 `event_id` 幂等、未完成任务重试继续结算
- [x] 媒体预授权与 `video_seconds` / `image_count` / `audio_seconds` 结算
- [x] HMAC 签名下载；默认 7 天清理
- [x] 钱包 `Reserve` 用 `UPDATE ... WHERE available_minor >= amount` 原子扣减
- [x] `scripts/e2e_m4.sh`、`TestM4MediaJobs`

### 验证记录（2026-08-29）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `go test ./...` | 通过 | `TestM4MediaJobs`：幂等不重提、签名下载、回调两次 2xx 且任务 completed、失败/取消释放预授权、图像 202；`TestM3BillingInvariants` 并发预授权仍 1 成 1 败 |
| `web` Vitest | 通过 | 既有钱包/品牌用例未回归 |
| `scripts/e2e_m0.sh`–`e2e_m3.sh` | 通过 | M4 未破坏既有切片 |
| `scripts/e2e_m4.sh` | 通过 | 视频 202、幂等同 job、签名 URL 含 sandbox payload、force-fail、取消、回调重试后 completed、图像、`/healthz` 为 `0.1.0-m4`、admin providers 含 `ark-seedance` |
| GitHub CI（`417f7be`） | 通过 | 首笔 `86321ce` 的 backend 因回调 `FirstOrCreate` 撞 `event_id` 唯一约束失败；修复后 push 与 PR 两次 `ci` 均全绿 |

`/healthz` 版本 `0.1.0-m4`；`/readyz` 含 `media` migration。Agent 无合并权限，需人工把 **#5** 合进 `release/v0.1.0`。

## M5 套餐、订阅与支付

文档验收：套餐发布自动校验和异常审核；权益按最早到期优先扣减；续费失败按重试/宽限期规则处理；支付 webhook 验签幂等；国内/国际支付订单都能发放和冲正权益。

实现清单：

- [x] 独立 `plans` 模块：套餐、订阅、权益账户与流水
- [x] 渠道套餐自动校验；低于 1 USD / 超额 / 高风险媒体配额进 `pending_review`
- [x] 权益 FIFO：即将过期赠送 → 当期套餐 → 现金钱包
- [x] billing 预授权只冻 `wallet_reserved_minor`，不直连套餐表
- [x] 独立 `payment` 模块：stripe / alipay / wechat / manual 沙箱适配器
- [x] webhook HMAC 验签，按 `external_event_id` 幂等；付费发放、退款冲正
- [x] Stripe 可自动续费；支付宝/微信到期 `past_due`，不伪造代扣
- [x] 续费失败：到期日、+1/+3/+5 天重试，7 天宽限后 `cancelled`
- [x] `scripts/e2e_m5.sh`、`TestM5PlansPayments`、`backend/openapi/m5.yaml`

### 验证记录（2026-08-29）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `go test ./...` | 通过 | `TestM5PlansPayments`：渠道低价进审核、webhook 重放不双发、FIFO 先扣即将过期赠送、无现金钱包也能 chat、支付宝不自动续费、Stripe `pm_fail` 进入 `past_due`、退款冲正未用权益、微信钱包入账 |
| `web` Vitest + `next build` | 通过 | 套餐价格展示；公共站/用户控制台套餐卡片 |
| `scripts/e2e_m0.sh`–`e2e_m4.sh` | 通过 | M5 未破坏既有切片 |
| `scripts/e2e_m5.sh` | 通过 | 公共套餐、渠道审核、Stripe 订阅+重放、权益覆盖 chat、支付宝/Stripe 续费失败、退款、微信充值、`/healthz` 为 `0.1.0-m5` |

`/healthz` 版本 `0.1.0-m5`；`/readyz` 含 `plans` 与 `payment` migration。Agent 无合并权限，需人工把 **#6** 合进 `release/v0.1.0`。

## M6 分销、佣金与渠道运营

文档验收：每个用户唯一归因；渠道额度不超发；佣金基于实际 usage；代理商只能看授权范围；退款同步冲正佣金；敏感身份脱敏且不暴露 prompt/completion。

实现清单：

- [x] 代理商 → 1 级 KOL → 2 级 KOL 种子树与推广码
- [x] 独立 `commission` 模块：策略、冻结明细、月度结算、人工打款
- [x] 无推广角色时保持 M3 批发价 10% 记到渠道
- [x] 渠道 `usd_credit` 额度不超发；佣金由平台承担
- [x] `/v1/partner/*` 按角色树过滤，邮箱脱敏，导出不含 prompt
- [x] `scripts/e2e_m6.sh`、`TestM6CommissionDistribution`

### 验证记录（2026-08-29）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `go test ./...` | 通过 | `TestM6CommissionDistribution`：KOL2 注册后 usage 拆出 frozen 的 direct/override/channel；代理商只看脱敏邮箱；退款冲正佣金；解冻后月结并人工打款；渠道额度收到 1 micro 后 Reserve 100 返回 `ErrInsufficientQuota` |
| `web` Vitest + `next build` | 通过 | 既有钱包/品牌/套餐用例未回归；渠道页增加额度与佣金卡片 |
| `scripts/e2e_m0.sh`–`e2e_m5.sh` | 通过 | M6 未破坏既有切片；M5 脚本按 `0.1.0-m` 前缀匹配版本 |
| `scripts/e2e_m6.sh` | 通过 | `THB-KOL2` 归因 reseller、冻结层级、代理商 CSV 无 prompt、退款 `reversed`、`ignore_minimum=1` 结算后人工 `paid`、额度清空后 chat 402、`/healthz` 为 `0.1.0-m6` |
| GitHub CI（`74216a6`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿 |

`/healthz` 版本 `0.1.0-m6`；`/readyz` 含 `commission` migration。Agent 无合并权限，需人工把 **#7** 合进 `release/v0.1.0`。

## M7 运营、运维与上线加固

文档验收：可按 Provider/模型/渠道/代理商/用户/API Key 查看成功率、延迟、错误、usage、成本、收入、毛利、佣金和待对账；关键故障有告警和 runbook；数据库可恢复到最近备份。

实现清单：

- [x] 独立 `ops` 模块：告警、runbook、备份演练、灰度
- [x] 看板按维度聚合网关流量和账务金额
- [x] API Key Redis 限流（RPM/并发）与 Provider 熔断
- [x] 审计检索、支付/媒体异常演练、灰度 Header
- [x] Provider/模型/路由/渠道管理 CRUD 与完整管理后台导航
- [x] 管理员 TOTP 2FA；敏感操作确认 + 可选 TOTP
- [x] Apache ECharts 看板、next-intl 中/英/日、OpenAPI 生成 TS Client
- [x] Grafana 业务/运行时 dashboard、WAL archive、加密异地备份脚本
- [x] `scripts/e2e_m7.sh`、`scripts/loadtest_limits.sh`、`scripts/backup_drill.sh`、`TestM7OpsHardening`
- [x] API Key 轮换/禁用/过期（`POST /v1/me/api-keys/{id}/rotate|disable|expire`）；过期或禁用后鉴权拒绝
- [x] 管理列表分页（`limit` 默认 20、最大 100 + `cursor`/`next_cursor`）
- [x] 对账导出：`GET /admin/billing/export`、`GET /admin/usage?format=csv`（含 `gross_profit`）
- [x] Playwright 组件/页面冒烟纳入 CI frontend job
- [x] 用户控制台 API Key：创建/查看/复制/轮换/禁用/过期（Zustand 提示 + Testing Library）
- [x] 上游 Base URL allowlist：拦截私网/元数据；生产仅 https + 白名单主机
- [x] OEM CNAME + 自动 HTTPS：`GET /v1/public/tls-check`、Caddy on-demand TLS、`POST /admin/brands/{id}/tls/issue`
- [x] 用户控制台用量与账单面板（`/v1/me/usage` + `/v1/me/ledger`）
- [x] 用户/管理媒体任务列表：`GET /v1/me/media`、`GET /admin/media?format=csv`（不含 prompt）
- [x] 管理列表导出 CSV（API Key / Provider / 媒体）与 shadcn/ui 基础组件
- [x] 审计/渠道/模型/路由/套餐/佣金/告警/runbook 列表统一分页 + `format=csv` + `q` 筛选
- [x] D23 管理角色隔离：`{admin}-finance|ops|tech|audit` bootstrap；财务不能看 Provider/轮换凭据；审计不能写退款
- [x] 文本网关 `Idempotency-Key` Redis 24h；相同键不同体 `409 idempotency_conflict`
- [x] 过期预授权 `ReapExpired` 回收余额（Worker 已挂）
- [x] 用户控制台会话创建媒体任务（不必贴 API Key）
- [x] Bifrost 沙箱 sidecar + `POST /admin/models/attach`；Compose 每日加密备份保留 30 天
- [x] 路由策略 `priority` / `weight` / `price` / `health`；健康优先跳过 maintenance，价格按 Provider 成本排序
- [x] 上游账号池：多 Key、状态/冷却/失效、指纹列表；无可用账号时跳过 Provider
- [x] 模型/价格同步进入 `draft`，审核后发布，弃用不删历史映射和价格
- [x] 用户控制台个人设置：`PATCH /v1/me`（显示名/zh|en|ja）、`POST /v1/me/password`；渠道仍不可自助切换
- [x] 指标时间序列与日报导出：`GET /admin/metrics/series`、`GET /admin/metrics/daily?format=csv`；ops 只调网关/账务接口
- [x] D34 接入示例：`/v1/public/docs-context` 含 Chat/Messages/视频与错误码说明；用户控制台可复制且不嵌入完整 Key
- [x] 渠道控制台本渠道用户列表；看板补充 P99 / fallback / 429 / 5xx
- [x] D24 预授权失败落库与媒体回调 P95：`preauth_failed`、`callback_latency_p95_ms`；ops 只调 billing/media 接口
- [x] D24 错误码分布、超时、Token/媒体用量与可配置告警阈值：`error_codes`/`timeouts`/`prompt_tokens`/`video_seconds`；`GET/PATCH /admin/ops/thresholds`
- [x] D33 渠道控制台套餐、推广链接与用量：`GET /channel/plans`、`GET/POST /channel/promotion-codes`、`GET /channel/usage`
- [x] D33 渠道结算：`GET /channel/settlements`、`GET /admin/settlements`；渠道只能看本渠道结算单
- [x] OEM TLS 沙箱门禁演练：`POST /admin/ops/drills/tls`、`scripts/tls_drill.sh`；已知域名 200、未知 404、沙箱 issued（不是公网 ACME）
- [x] D33 渠道归因汇总：`GET /channel/attribution` 按推广码/层级点数；渠道 B 看不到官方 THA1
- [x] D33 代理商/KOL 分层门户：`GET /v1/partner/me`、bootstrap `{admin}-kol1|kol2`；2 级看不到 `THB-AGENT` 用户，代理商能看整棵树
- [x] D33 公共站模型目录与充值订阅：`GET /v1/public/models` 不含 Provider；首页可兑换/下单/订阅
- [x] D33 平台管理控制台套餐审核 / 支付 / 佣金策略：`/admin/plans` 通过或拒绝 `pending_review`；`GET /admin/payments`；`GET/PATCH /admin/commission-policy`
- [x] D33 公共站未登录购买引导登录：首页「去登录」带 `next=/`，登录后回跳；拒绝 `//` 外站开放重定向
- [x] D33 管理后台价格与指标：`GET/POST /admin/price-books`、`/admin/prices`、`/admin/metrics`
- [x] D01/D02 用户封禁与人工改归因：`POST /admin/users/{id}/ban|unban|attribution`；缺确认 409；封禁后登录/会话/API Key 403；未结算佣金 `held`；`/admin/users` 可操作
- [x] D24 告警与 runbook 管理页：`/admin/alerts` 评估 `GET/POST /admin/ops/alerts`；`/admin/runbooks` 列出处置步骤
- [x] D01/D02 财务操作面：`/admin/billing` 退款/确认入账/赠送额度；`/admin/channels` 调整渠道额度；`/admin/commission` 解冻、月结、人工打款；赠送与解冻补二次确认和审计
- [x] D24 运维开关与 usage 回放管理面：`/admin/settings` 健康探测、打开/复位熔断、读写灰度（不强制确认头）；`/admin/usage` 按 request_id 回放；`POST /admin/usage/replay` 缺确认 409 并写审计
- [x] D33 推广角色/推广码管理页：`/admin/promos` 创建 `acquisition` 角色与推广码；`POST /admin/acquisition-roles` 与 `POST /admin/promotion-codes` 缺确认 409 并写审计
- [x] D24 Provider 列表行内健康探测：`/admin/providers` 每行 `POST /admin/providers/{id}/health-check`，不会计费、不强制确认头
- [x] D02 Provider 凭据轮换管理面：`/admin/providers` 表单 `POST /admin/providers/{id}/credentials`；二次确认；响应只回 `credential_ref`，成功后清空密文
- [x] D24 备份演练与 OEM 证书管理面：`/admin/settings`「备份演练」走 `POST /admin/ops/backup-drill`（不强制确认）；「OEM 证书」读取 `GET /admin/brands` 并 `POST /admin/brands/{id}/tls/issue`（需确认，沙箱 `issued`）
- [x] D02 账号池与模型挂载/弃用管理面：`/admin/providers`「账号池」读取指纹、添加、冷却、停用；`/admin/models` 可 `attach` / `deprecate`，弃用不删历史映射和价格
- [x] D24 路由策略管理面：`/admin/routes` 可 `POST/PATCH` 创建路由组并改 `priority`/`weight`/`price`/`health`；创建和改策略需确认；不要改 `rg_echo`
- [x] D24 支付/媒体/TLS 异常演练管理面：`/admin/settings`「异常演练」走 `POST /admin/ops/drills/payment|media|tls`（不强制确认）
- [x] D06 佣金重算管理面：`/admin/commission`「佣金重算」走 `POST /admin/commissions/recalc`；缺确认 409
- [x] D01/D33 渠道创建/改状态管理面：`/admin/channels` 可 `POST/PATCH` 创建渠道并改状态；缺确认 409；不要停用官方/代理商/OEM 种子渠道
- [x] D33 套餐创建/下架管理面：`/admin/plans` 可 `POST` 创建、`PATCH` 下架为 `archived`；不要下架 `pln_echo_month`
- [x] D02 Provider 改状态/RPM 管理面：`/admin/providers` 可 `PATCH` 改 `status`/`rpm_limit`；缺确认 409；不要改种子 echo/gemini
- [x] D23 管理员 2FA 管理面：`/admin/settings` 可读取/绑定/启用/关闭 TOTP；关闭缺确认 409；启用后再关闭还要 TOTP；不要在共享管理员上留下 enabled
- [x] D02 管理员禁用 API Key：`/admin/keys` 可 `POST /admin/api-keys/{id}/disable`；缺确认 409；响应不回密文；禁用后网关 403
- [x] D12 续费扫描管理面：`/admin/plans` 可强制到期并 `POST /admin/subscriptions/process-renewals`；生产禁止；不要对演示订阅乱拨时钟
- [x] D24 Outbox 统计与审计探测管理面：`/admin/audit` 可 `GET /admin/outbox/stats` 与 `POST /admin/audit-probes`；生产禁止探测；不强制确认
- [x] D3.3 管理员手工创建模型：`/admin/models` 可 `POST /admin/models`；缺确认 409；默认 `draft`；不要改 `tokenhub/echo-1`
- [x] D19/D20 渠道自建套餐：渠道控制台 `POST /channel/plans`；归属强制当前渠道；低于 1 USD 进 `pending_review`；不要改 `pln_echo_month`
- [x] D13 API Key 模型白名单：`POST /v1/me/api-keys` 写 `allowlist`/`rpm_limit`；`GET` 列表回带白名单；空名单不限制；用户控制台可填逗号分隔模型；不在名单内聊天返回 `403 model_not_allowed`
- [x] D13 API Key 并发限额：`POST /v1/me/api-keys` 写 `concurrency_limit`（默认 5）；列表回带；占满 Redis 并发槽后聊天 `429 rate_limited`；用户控制台可填并发

### 验证记录（2026-08-29）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `go test ./...` | 通过 | `6e03247`：`TestM7OpsHardening` 含 Key 轮换旧密文 403、禁用/过期 403、billing export 含 `gross_profit`、元数据 Base URL 400、OEM `tls-check` 200/404 |
| `web` Vitest + `next build` + Playwright | 通过 | 12 个 Vitest（含 Testing Library `KeysList`）；`next build` 成功；Playwright 管理导航 + 用户控制台 API Key |
| `scripts/e2e_m0.sh`–`e2e_m6.sh` | 通过 | 2026-08-29 用最新 `0.1.0-m7` API 重跑，未回归 |
| `scripts/e2e_m7.sh` | 通过 | 轮换旧 Key 403、禁用 403、export/`usage.csv`、`limit=5` 含 `next_cursor`、169.254 Base URL 400、未知域名 TLS 404、`loadtest_limits` 出现 429；`05f1af5` 增加财务 403 Provider、审计不能退款、聊天幂等 409、会话创建媒体 |
| GitHub CI（`50f5ea1`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（含 Playwright） |
| `05f1af5` / `2920cbb` 角色/幂等/Bifrost | 通过 | 本地 e2e + GitHub CI `2920cbb` 于 2026-08-29 全绿 |
| GitHub CI（`ccb243c` / `e197591`） | 通过 | 路由策略与账号池文档提交于 2026-08-29 全绿（4 checks） |
| 路由策略 `priority`/`weight`/`price`/`health` | 通过 | `ccb243c`：`go test ./...`（`TestApplyStrategy`、`TestM2` 健康/价格、`TestM3` 不被测试价污染）；本地 `e2e_m0`/`e2e_m2`/`e2e_m7` 通过（health 策略落到 echo-backup 后复位 priority） |
| 上游账号池 | 通过 | `4525e54`：`TestAccountUsable`、`TestM7` 冷却 503 / 活跃账号路由 / 401 invalid / 财务 403；本地 `e2e_m0`/`e2e_m2`/`e2e_m7` 通过（`catalog` migration `0003_account_pool`） |
| 模型同步 draft/审核/发布/弃用 | 通过 | `e8a6f00`：`TestM7` + 本地 `e2e_m0`/`e2e_m2`/`e2e_m7`（draft 不进 `/v1/models`，publish 后可见，deprecate 后对客户隐藏、管理列表保留） |
| tool calls / JSON schema / vision / reasoning | 通过 | `a0ae4bd`：`TestValidateChat` + `TestM2` + 本地 `e2e_m2`/`e2e_m7`（tool_calls、json、vision、reasoning_tokens、Anthropic tool_use；logit_bias 仍 400） |
| `9abc675` 媒体列表 + shadcn | 通过 | `TestM4` 列出本用户任务且 admin CSV 无 prompt；`e2e_m1/m4/m7` 与 Playwright「媒体任务」通过 |
| 个人设置 + 指标日报 | 通过 | `30ceac9`/`fa31f55`：`TestNormalizeLocale`、`TestMergeDaily`、`TestM1` 改资料/改密、`TestM7` 7 日序列+CSV；本地 `e2e_m0`/`e2e_m1`/`e2e_m7` 与 Playwright「个人设置」通过 |
| GitHub CI（`9cfbdcf` / `32b257d`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；同分支 `2459260` 工具调用文档提交亦全绿 |
| D34 接入示例 + 渠道用户 + P99/429 | 通过 | `96f1104`/`a6c1493`：`TestDocsExamplesUseBrandAndPlaceholderKey`、`TestM1` OEM 文档含 `$TOKENHUB_API_KEY`/`/v1/messages`、`TestM7` totals 含 `latency_p99_ms`/`http_429`；本地 `e2e_m0`/`e2e_m1`/`e2e_m7` 与 Playwright「接入示例」「本渠道用户」通过 |
| D24 预授权失败 + 回调 P95 | 通过 | `5722b23`：`TestM3` 402 后 `PreauthFailed >= 1`；`TestM7` totals 含 `preauth_failed`/`callback_latency_p95_ms`；本地 `e2e_m7` dashboard 含这两个字段 |
| D24 错误码/超时/用量 + 告警阈值 + D33 渠道套餐/推广 | 通过 | `dd796c2`/`29243e8`：`TestClampThresholds`、`TestM7` prompt_tokens/timeout fallback/`error_codes.timeout`、阈值 PATCH；`TestM1` 渠道套餐/推广码隔离；本地 `e2e_m0`/`e2e_m1`/`e2e_m7`、Vitest 13、Playwright 3（含「本渠道套餐」「推广链接」「本渠道用量」）通过 |
| D33 渠道结算 + OEM TLS 门禁演练 | 通过 | `7fd4640`：`TestM1` 结算隔离、`TestM7` tls drill；本地 `e2e_m0`/`e2e_m1`/`e2e_m7`、`scripts/tls_drill.sh`、Vitest 13、Playwright 3（含「本渠道结算」）。`46f1109`/`08d2d3f` 修了 Prometheus/导出 `curl\|grep -q` SIGPIPE（退出码 23） |
| D33 渠道归因汇总 | 通过 | `0fa61db`：`TestM1` 未登录 403、渠道 B 有 THB1 无 THA1；本地 `e2e_m0`/`e2e_m1`/`e2e_m7`、Vitest 13、Playwright 3（含「本渠道归因」）；`GET /channel/attribution` 200 |
| 浏览器 CORS localhost/127.0.0.1 | 通过 | `134caf7`：`TestOriginAllowedLoopbackAlias`；`Origin: http://127.0.0.1:3000` OPTIONS/GET 带回同一 Origin；evil.example / oem.localhost 无 ACAO。浏览器点「刷新归因/结算」得到 403「未授权」，不再被 CORS 拦住 |
| GitHub CI（`84bd51e`–`134caf7`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含结算/TLS、e2e SIGPIPE、归因、CORS |
| D33 代理商/KOL 分层门户 | 通过 | `a931fb2`：`TestM6` 未登录/普通用户 403，agent 同时看到 THB-AGENT 与 THB-KOL2，kol2/kol1 看不到 THB-AGENT；本地 `e2e_m0`/`e2e_m1`/`e2e_m6`/`e2e_m7`、Vitest 13、Playwright 4（含「我的层级」「范围内用户」） |
| GitHub CI（`6f5056b` / `a931fb2`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks） |
| D33 公共站模型目录与充值订阅 | 通过 | `7a7d937`：`TestM1` 公开模型含 echo-1、OEM 含 oem-demo、无 providers；本地 `e2e_m0`/`e2e_m1`/`e2e_m7`、Vitest 13、Playwright 5（含「可用模型」「套餐与订阅」「充值」） |
| D33 管理后台套餐审核 / 支付 / 佣金策略 | 通过 | `ac567f9`：`TestValidatePolicyRejectsOverCap`、`TestM5` 待审队列+支付单列表、`TestM6` 策略缺确认 409 / PATCH 后还原；本地 `e2e_m0`/`e2e_m1`/`e2e_m5`/`e2e_m6`/`e2e_m7`、Vitest 13、Playwright 6。浏览器登录 `admin@tokenhub.local` 后：`Browser Review Cheap` 从 `pending_review` 点「通过」变为 `published`；佣金页读取 `m6-v1`；支付页列出 stripe/alipay/wechat |
| GitHub CI（`5a8122a` / `ac567f9`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；`5a8122a` 是公共站文档，`ac567f9` 含套餐审核/支付/佣金页与 Playwright 6 |
| GitHub CI（`08bff9b`） | 通过 | 文档提交 4 checks 全绿 |
| D33 登录回跳 + 价格/指标页 | 通过 | `71cdf98`：`TestM3` 价格书含 published echo；`safeNextPath` 拒绝 `//`；本地 `e2e_m0`/`m1`/`m3`/`m7`、Vitest 14、Playwright 6。浏览器：首页「去登录」→ `/login?next=%2F` → 回首页；`/admin/prices` 列出 published/superseded；`/admin/metrics` 显示运营看板 |
| 用户封禁 / 改归因 / 告警手册 | 通过 | `98e8c43`：`TestM1` 缺确认 409、封禁后 login/session/Key 403、解封后可登录、改归因到 THB1；`TestM6` 未结算佣金 `held` 再释放。本地 `e2e_m0/m1/m2/m6/m7`、Vitest 14、Playwright 6。浏览器：`browser-ban-1788035722@example.test` 封禁 → 归因 THB1/`chn_reseller_b` → 解封；`/admin/alerts` 评估熔断告警；`/admin/runbooks` 列出备份/对账/熔断手册 |
| GitHub CI（`067e1fc` / `98e8c43`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；`067e1fc` 是登录回跳文档，`98e8c43` 含封禁/归因/告警手册 |
| 财务操作面：退款/赠送/额度/打款 | 通过 | `3c1eb73`：`TestM5` 赠送缺确认 409；`TestM6` 解冻缺确认 409。本地 `e2e_m0/m1/m5/m6/m7`、Vitest 14、Playwright 6。浏览器：赠送 `1000000` usd_credit 给 `usr_b6e100e20dda47428641c96e8c4ce24c`；`chn_reseller_b` 额度 +1；佣金页读取 `m6-v1` 并展示手工结算 |
| GitHub CI（`320b051` / `3c1eb73`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；`320b051` 是封禁文档，`3c1eb73` 含财务操作面 |
| GitHub CI（`061ed23`） | 通过 | 财务操作面文档提交 4 checks 全绿 |
| 运维开关 + usage 回放管理面 | 通过 | `f561a52`：`TestM3` 回放缺确认 409；本地 `e2e_m0`–`e2e_m7`、Vitest 14、Playwright 6（含「运维开关」「健康探测」「回放 usage」）。浏览器：`/admin/settings` 探测 `prd_echo_primary` → `健康=available`，复位熔断，读取灰度 `echo-backup 0%`；`/admin/usage` 回放 `req_46cbb66a13f54fc7865ed35fea21803c`（原 `pending_reconciliation`）→ `confirmed` `usg_dac03ff2edc34948b14e25467f81e5ad`，审计 `billing.usage.replay` |
| GitHub CI（`f561a52`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含运维开关与 usage 回放 |
| 推广角色/推广码管理页 | 通过 | `ccf5914`：`TestM1` 创建角色/码缺确认 409；本地 `e2e_m0`–`m3`/`m5`–`m7`、Vitest 14、Playwright 6（含导航「推广码」与「创建推广码」）。浏览器：`/admin/promos` 创建 `kol_l2` `acr_5c48d3e12fde43a391a7dbd66ae9ffc9`，推广码 `THB-BRW-829`，审计 `identity.promotion.create`。`8ed7e4f` 去掉续费扫描 100 条上限，避免共享库漏处理支付宝手动续费 |
| GitHub CI（`8ed7e4f`） | 通过 | 续费扫描修复 4 checks 全绿 |
| Provider 列表行内探测 | 通过 | `71eac51`：Vitest 14、Playwright 6（「列表每行可探测」）、`e2e_m0`/`e2e_m1`/`e2e_m7`。浏览器：`/admin/providers` 对 `echo-primary` 点「探测」得到 `available`。`284e76f` 把 e2e_m7 的 `curl\|grep -q` 改成先下载再检查，避免 SIGPIPE |
| Provider 凭据轮换管理面 | 通过 | `ffb2cb5`：`TestM7` 缺确认 409、响应无明文；本地 `e2e_m1`/`e2e_m7`、Vitest client、Playwright 6（「凭据轮换」「轮换凭据」）。浏览器对一次性提供商 `prd_a3292c66a06445efa84a6e43d354b175` 轮换得到 `crd_733979898aa247d1926fc85438fe8474`，密文不回显 |
| GitHub CI（`d973cca` / `a25df1f` / `6794874` / `4363dd3` / `ffb2cb5` / `93d1e31`） | 通过 | 行内探测文档与凭据轮换（含 FormData / 占位符 / `currentTarget` 修复及进度文档）于 2026-08-29 各 4 checks 全绿 |
| GitHub CI（`75bab92` / `e520d90` / `f299380` / `e05f51c` / `cac16f2` / `a03ae54`） | 通过 | 设置页备份演练与 OEM 证书、TLS 缺确认 e2e、以及对应进度文档于 2026-08-29 各 4 checks 全绿 |
| GitHub CI（`72f7dcb` / `f92ae2c` / `e07a43a` / `e1b32ab`） | 通过 | 账号池与模型挂载/弃用（含添加/冷却文案不被 reload 盖掉）及进度文档于 2026-08-29 各 4 checks 全绿 |
| 备份演练 + OEM 证书管理面 | 通过 | `e520d90`：`TestM7` 签发缺确认 409；本地 `e2e_m1`/`e2e_m7`、Vitest、Playwright 6（「备份演练」「签发证书」）。浏览器：演练 `passed` RPO 15 / RTO 60；`brd_oem` 签发 `issued` / `edge.tokenhub.local` |
| 账号池 + 模型挂载/弃用 | 通过 | `e07a43a`：本地 Playwright 6、`e2e_m1`/`e2e_m7`。浏览器对 `prd_d0f29e157f8c493b88a929aa73061bb0` 读取指纹后冷却；`/admin/models` 挂载 `tokenhub/echo-1`，密文不回显 |
| 路由 POST/PATCH + 异常演练 + 佣金重算 | 通过 | `285cc3b`：`TestM7` 创建/改路由缺确认 409、重算缺确认 409；本地 `e2e_m1`/`e2e_m7`、Vitest 14、Playwright 6（「创建路由」「保存策略」「支付演练」「TLS 演练」「重算佣金」）。浏览器：一次性模型 `tokenhub/ops-ui-1788044138` 创建 `rg_48f052dc3d3c40e2a5edc7aad58ccb39` 并改成 `health`；`rg_echo` 仍是 `priority`；支付/媒体/TLS 演练 `passed`；重算 `usg_1d71b9926bf840db8f8fe156ee10212a` → `com_77755a22279c45a6ac9aac71aa2e6ef0` `frozen` / `m3-stub-v1` |
| GitHub CI（`285cc3b` / `be00f29`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含路由/演练/重算管理面及进度文档 |
| 渠道创建/改状态管理面 | 通过 | `d25be71`：`TestM7` 创建/改渠道缺确认 409；本地 `e2e_m1`/`e2e_m7`、Vitest 14、Playwright 6（「创建渠道」「保存渠道」）。浏览器创建 `chn_384ea1b17e86444f9b1527fbd76227cd` `ops-ui-1788044532` 后停用；`chn_official_a` / `chn_reseller_b` / `chn_oem_c` 仍是 `active` |
| GitHub CI（`d25be71`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含渠道创建/改状态管理面 |
| 套餐创建/下架管理面 | 通过 | `6d66c36`：`TestM7` 平台套餐直接 `published` 再 `archived`；本地 `e2e_m1`/`e2e_m5`/`e2e_m7`、Vitest 14、Playwright 6（「创建套餐」「下架套餐」）。浏览器创建 `pln_ebc217695ed24f108b726764bf6964b2` `Ops UI Plan 1788044786` 后下架；`pln_echo_month` 仍是 `published` |
| GitHub CI（`6d66c36`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含套餐创建/下架管理面 |
| Provider 改状态/RPM 管理面 | 通过 | `5196e73`/`116f864`：`TestM7` 改 Provider 缺确认 409，带确认后 `maintenance` / RPM 30；本地 `e2e_m1`/`e2e_m7`、Vitest 14、Playwright 6（「改 Provider 状态」「保存 Provider」）。浏览器把一次性 `prd_124893ce4c8243bb9443da8193d2d02c` `ops-ui-1788045155` 改成 `maintenance` / RPM 30；`prd_echo_primary` / `prd_echo_backup` / `prd_gemini` 仍是 `active` |
| GitHub CI（`116f864`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含 Provider 改状态/RPM 与编译修复 |
| 管理员 2FA 管理面 | 通过 | `b1d83dd`：`TestM7` 关闭缺确认 409、启用后缺 TOTP 409、带确认+TOTP 后 `disabled`；本地 `e2e_m1`/`e2e_m7`、Vitest 14、Playwright 6（「读取 2FA」「确认启用」「关闭 2FA」）。浏览器绑定后启用再关闭；`GET /admin/me/2fa` 最终 `disabled` / `enabled=false` |
| GitHub CI（`6e1d635` / `b1d83dd`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含 Provider 文档与管理员 2FA 管理面 |
| 管理员禁用 API Key | 通过 | `73dba1a`：`TestM7` 管理员禁用缺确认 409，禁用后聊天 403；本地 `e2e_m1`/`e2e_m7`、Vitest 14、Playwright 6（「禁用 API Key」「禁用 Key」）。浏览器禁用一次性 `key_ecc88c1ccc474b99ad5819b9f5a8202f` `thk_3879` → `disabled`，列表无密文，原 Key 聊天 403 |
| GitHub CI（`f9f59d4` / `73dba1a`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含 2FA 文档与管理员禁用 API Key |
| 续费扫描管理面 | 通过 | `ae90f2d`/`f9e9e83`：`TestM7` 不存在订阅强制到期 400，扫描返回 `processed`；本地 `e2e_m1`/`e2e_m5`/`e2e_m7`、Vitest 14、Playwright 6（「续费扫描」「强制到期」）。浏览器对 `sub_missing` 得到「无法改到期时间」，续费扫描 `processed=135` |
| GitHub CI（`f9e9e83`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；`ae90f2d` 因缺失订阅仍 200 失败，`f9e9e83` 修成 400 后全绿 |
| Outbox 统计 + 审计探测管理面 | 通过 | `31e384d`：`TestM7` 先读 stats 再写 `audit.probe`；本地 `e2e_m0`/`e2e_m1`/`e2e_m7`、Vitest 14、Playwright 6（「读取 Outbox」「写入探测」）。浏览器：`/admin/audit` 读取 `pending=0 published=14497 failed=0`，写入探测 `aud_29edb4ed6489458d83562324141ce3dd` → `audit.probe` |
| GitHub CI（`5db7e1f`） | 通过 | 续费扫描文档提交 4 checks 全绿 |
| GitHub CI（`31e384d`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-29 全绿（4 checks）；含 Outbox 统计与审计探测管理面 |
| GitHub CI（`b95f796`） | 通过 | Outbox 进度文档提交 4 checks 全绿 |
| 管理员手工创建模型 | 通过 | `56e6537`：`TestM7` 创建缺确认 409，带确认后 `draft`；本地 `e2e_m0`/`e2e_m1`/`e2e_m2`/`e2e_m7`、Vitest 14、Playwright 6（「创建模型」）。浏览器创建 `tokenhub/ops-ui-1788049121849` → `draft`；`tokenhub/echo-1` 仍是 `published` |
| GitHub CI（`56e6537`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-30 全绿（4 checks）；含管理员手工创建模型 |
| 渠道自建套餐 | 通过 | `9d7802a`/`9eff44b`：`TestM1`/`TestM5` 渠道创建低价套餐进 `pending_review`，归属被强制成 `chn_reseller_b`；bootstrap `channel.b@tokenhub.local` 可用 `password1` 登录；本地 `e2e_m0`/`e2e_m1`/`e2e_m5`/`e2e_m7`、Vitest 14、Playwright 6（「创建渠道套餐」）。浏览器创建 `pln_d3edc979c42b48198ccd597bebb36218` → `pending_review` / `below_price_floor`；`pln_echo_month` 仍是 `published` |
| GitHub CI（`9eff44b`） | 通过 | `ci / backend` 与 `ci / frontend` 于 2026-08-30 全绿（4 checks）；含渠道自建套餐与 bootstrap 登录密码 |
| D13 API Key 模型白名单 | 通过 | `6758aff`：`TestNormalizeAllowlist`、`TestM2` 创建/列表回带 `google/gemini-flash`，聊 `tokenhub/echo-1` 得 `403 model_not_allowed`，echo-only Key 仍 200；本地 `e2e_m0`/`e2e_m1`/`e2e_m2`/`e2e_m7`、Vitest 16、Playwright 6（「模型白名单」）。`b8967b2` 把 e2e_m7 的种子套餐检查改成未分页 CSV。浏览器用 `allowlist-ui-1788050403@example.test` 创建 `key_2154770b1fa84ab8ab37898129824b8b` `gemini-only` → 白名单 `google/gemini-flash` / RPM 60；该 Key 聊 echo 为 `403 model_not_allowed`，聊 gemini 200 |
| GitHub CI（`469dced`） | 通过 | 白名单标签修复于 2026-08-30 全绿（通知 2 checks；随后合入 release） |
| 合入 `release/v0.1.0` #4 squash | 通过 | `045a7f3`：59 个 add/add 全部保留 M7；`docs/05` 去掉重复账务段 |
| D13 API Key 并发限额 | 通过 | `fe7bd5f`：`TestM2` 创建 `concurrency_limit=1`，占 Redis 槽聊 echo `429 rate_limited`，释放后 200；本地 `e2e_m1`/`e2e_m2`/`e2e_m7`、Vitest、Playwright 6（「并发限额」）。浏览器用 `conc-ui-1788052125@example.test` 创建 `key_0cbefed62a034c19a7786ede874a655d` `one-slot` → 并发 1；占槽 429「超过并发限额」，释放后 echo-primary |

`/healthz` 版本 `0.1.0-m7`；`/readyz` 含 `ops` migration。Agent 无合并权限，需人工按 **#4 → #5 → #6 → #7 → #8** 合进 `release/v0.1.0`。M5 CI 已在 `5febdf7`/`b4ef09e` 全绿（#6）；M6 CI 已在 `74216a6` 全绿（#7）。

## 后续

P0 里程碑全部落地后，P1 项仍按 `08` 第 4 节保留，不在本轮缩小或提前宣称完成。仍待人工按 **#4 → #5 → #6 → #7 → #8** 合入 `release/v0.1.0` 后在基线上重跑全量 e2e。公网 Let's Encrypt ACME 仍由边缘节点签发，本仓库只覆盖沙箱门禁演练。
